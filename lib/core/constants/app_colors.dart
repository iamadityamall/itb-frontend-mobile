import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF3F7D58);
  static const secondary = Color(0xFFEF9651);
  static const ternary = Color(0xFFFFFFFF);

  static const textBlack = Color(0xFF222222);
  static const textWhite = Color(0xFFFBFBFB);
  static const textGrey = Colors.black45;
  static const blue = Colors.blue;


  // drawer colors
  static const drawerUsernameColor = ternary;
  static const drawerHeadingColor = ternary;
  static const drawerSubHeadingColor = ternary;
  static const drawerSubHeadingArrowColor = ternary;
  static const drawerHeaderColor = primary;

  // logout colors
  static const appbarLogoutDialogueBoxTextColor = textWhite;
}
