import 'package:flutter/material.dart';

class TabFour extends StatelessWidget {
  const TabFour({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Investments Screen"));
  }
}
